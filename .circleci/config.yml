
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - run: echo "hello world 1"
  test:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - run: echo "hello world 2"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build:
          requires: 
            - test
      - test
